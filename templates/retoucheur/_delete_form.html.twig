<form method="post" action="{{ path('app_admin_retoucheur_delete', {'id': retoucheur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ retoucheur.id) }}">
    <button class="btn">Delete</button>
</form>
