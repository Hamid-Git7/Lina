{% extends 'base.html.twig' %}

{% block title %}Couleur{% endblock %}

{% block body %}
    <h1>Couleur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ couleur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ couleur.nom }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ couleur.createdAt ? couleur.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ couleur.updatedAt ? couleur.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ couleur.deletedAt ? couleur.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_couleur_index') }}">back to list</a>

    <a href="{{ path('app_admin_couleur_edit', {'id': couleur.id}) }}">edit</a>

    {{ include('couleur/_delete_form.html.twig') }}
{% endblock %}
