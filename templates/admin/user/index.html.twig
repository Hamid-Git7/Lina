{% extends 'base.html.twig' %}

{% block title %}User index
{% endblock %}

{% block body %}
	<section id="caftan">
		<h1 class="user" >Utilisateurs</h1>

		<a class="btn-all" href="{{ path('app_admin_user_new') }}">Créer</a></button>
		<div class="header_fixed mt-4 mb-4">
			<table>
				<thead>
					<tr>
						<th>Id</th>
						<th>Email</th>
						<th>Roles</th>
						<th>actions</th>
					</tr>
				</thead>
				<tbody>
					{% for user in users %}
						<tr>
							<td>{{ user.id }}</td>
							<td>{{ user.email }}</td>
							<td>{{ user.roles ? user.roles|json_encode : '' }}</td>
							<td>
								<a class="btn-all" href="{{ path('app_admin_user_show', {'id': user.id}) }}">show</a>
								<a class="btn-all" href="{{ path('app_admin_user_edit', {'id': user.id}) }}">edit</a>
							</td>
						</tr>
					{% else %}
						<tr>
							<td colspan="8">no records found</td>
						</tr>
					{% endfor %}
				</tbody>
			</table>
		</div>
	</section>
{% endblock %}
