<form method="post" action="{{ path('app_robe_delete', {'id': robe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ robe.id) }}">
    <button class="btn">Delete</button>
</form>
