{% extends 'base.html.twig' %}

{% block title %}Taille index{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Taille index</h1>

    <button type="button" class="btn btn-dark"><a href="{{ path('app_admin_taille_new') }}">Create new</a></button>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>DeletedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for taille in tailles %}
            <tr class="table-success mt-4">
                <td>{{ taille.id }}</td>
                <td>{{ taille.nom }}</td>
                <td>{{ taille.createdAt ? taille.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ taille.updatedAt ? taille.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ taille.deletedAt ? taille.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_admin_taille_show', {'id': taille.id}) }}">show</a>
                    <a href="{{ path('app_admin_taille_edit', {'id': taille.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
