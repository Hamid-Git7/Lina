<form method="post" action="{{ path('app_retouche_delete', {'id': retouche.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ retouche.id) }}">
    <button class="btn">Delete</button>
</form>
